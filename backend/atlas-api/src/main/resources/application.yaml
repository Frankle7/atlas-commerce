spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/atlas_commerce
    username: atlasADMIN
    password: atlas123ADM

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true